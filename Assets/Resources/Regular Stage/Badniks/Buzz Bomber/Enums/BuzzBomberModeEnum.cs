using UnityEngine;

public enum BuzzBomberMode
{
    [Tooltip("Idle Mode = 0")]
    Idle,
    [Tooltip("Walking Mode = 1")]
    Flying,
    [Tooltip("Firing Mode = 2")]
    Firing,
    [Tooltip("Escape Mode = 3")]
    Escape
}

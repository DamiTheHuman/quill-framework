using UnityEngine;
public enum CrabmeatMode
{
    [Tooltip("Idle Mode = 0")]
    Idle,
    [Tooltip("Walking Mode = 1")]
    Walking,
    [Tooltip("Waiting Mode = 2")]
    Waiting,
    [Tooltip("Firing Mode = 3")]
    Firing,
}
